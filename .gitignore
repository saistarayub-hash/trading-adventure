# dependencies (Electron shell)
node_modules/

# runtime data created by the companion / server
.data/
companion/.data/
users.json
lessons.json
knowledge.json
*.log

# mobile export + generated native projects (Capacitor)
mobile/www/
mobile/android/
mobile/ios/
mobile/node_modules/

# installer output (electron-builder)
companion/release/

# secrets — the signing key auth.js generates must never be committed
.secret
.secret.*
*.key
.env

# os junk
.DS_Store
Thumbs.db

# downloaded APK artifacts
apk/
